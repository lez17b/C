The Big O analysis of the clear() method within my stack implementation would be O(n) because the it only calls the method v.clear() which also calls the destructor method for each of the items on the stack, making it an O(n) time complexity algorithm.
